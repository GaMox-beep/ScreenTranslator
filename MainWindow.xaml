<Window x:Class="ScreenTranslator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ScreenTranslator.ViewModels"
        Title="ScreenTranslator - Cài đặt" 
        Width="480" Height="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Nhóm 1: Google Gemini API -->
        <GroupBox Grid.Row="0" Header="Cấu hình Google Gemini AI" Margin="0,0,0,10" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="85"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- API Key -->
                <Label Grid.Column="0" Content="API Key:" VerticalAlignment="Center"/>
                <PasswordBox x:Name="TxtApiKey" Grid.Column="1" Height="26" 
                             VerticalContentAlignment="Center"
                             PasswordChanged="TxtApiKey_PasswordChanged"/>
            </Grid>
        </GroupBox>

        <!-- Nhóm 2: Ngôn ngữ -->
        <GroupBox Grid.Row="1" Header="Ngôn ngữ" Margin="0,0,0,10" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <Label Content="Nguồn:" Padding="0,0,0,3"/>
                    <ComboBox Height="26" 
                              ItemsSource="{Binding SourceLanguages}" 
                              SelectedItem="{Binding SelectedSourceLanguage}"/>
                </StackPanel>

                <TextBlock Grid.Column="1" Text="➔" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,5"/>

                <StackPanel Grid.Column="2">
                    <Label Content="Đích:" Padding="0,0,0,3"/>
                    <ComboBox Height="26" 
                              ItemsSource="{Binding TargetLanguages}" 
                              SelectedItem="{Binding SelectedTargetLanguage}"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Nhóm 3: Phím tắt -->
        <GroupBox Grid.Row="2" Header="Phím tắt quét màn hình" Margin="0,0,0,10" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ComboBox Grid.Column="0" Height="26" 
                          ItemsSource="{Binding Modifiers}" 
                          SelectedItem="{Binding SelectedModifier}"/>

                <TextBlock Grid.Column="1" Text="+" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                <ComboBox Grid.Column="2" Height="26" 
                          ItemsSource="{Binding Keys}" 
                          SelectedItem="{Binding SelectedKey}"/>
            </Grid>
        </GroupBox>

        <!-- Nút thao tác & Trạng thái -->
        <DockPanel Grid.Row="4" LastChildFill="False">
            <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" DockPanel.Dock="Left">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="#107C41"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsStatusError}" Value="True">
                                <Setter Property="Foreground" Value="#DC2626"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <Button x:Name="BtnSave" Content="Lưu Cài Đặt" Width="100" Height="28" 
                    DockPanel.Dock="Right" Click="BtnSave_Click"/>
            <Button x:Name="BtnTestCapture" Content="Chụp Thử" Width="90" Height="28" 
                    Margin="0,0,10,0" DockPanel.Dock="Right" Click="BtnTestCapture_Click"/>
        </DockPanel>
    </Grid>
</Window>
